# SHM internal URL (how our backend reaches SHM API from inside Docker)
SHM_INTERNAL_URL=http://host.docker.internal:8082

# PostgreSQL
POSTGRES_USER=support
POSTGRES_PASSWORD=changeme
POSTGRES_DB=support

# MinIO
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=changeme_minio

# CORS — your billing domain
CORS_ORIGIN=https://bill.yourdomain.com
